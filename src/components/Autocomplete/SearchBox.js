import styled from 'styled-components';

const SearchBox = styled.div`
  position: relative;
  display: flex;
  flex-direction: row;
  width: calc(100% - 1rem);
  padding: 0.5rem;
`;

export default SearchBox;
