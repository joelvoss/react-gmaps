import styled from 'styled-components';

const Wrapper = styled.ul`
  position: relative;
  list-style: none;
  flex: 0 0 40%;
  margin: 0;
  padding: 0;

  overflow: auto;
`;

export default Wrapper;
